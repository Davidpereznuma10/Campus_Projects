// Program that enters by keyboard:
// a. the value of the side of a square to display on the screen the
// perimeter of the same
// b. the base and height of a rectangle to show the area of ​​the same
const  side = parseFloat(prompt("Type the side of square"));
const square = alert(`How the side measure is ${side}cm, and the parimeter measure is ${side*4}cm`)

const base = parseFloat(prompt("Type the rectangle base"));
const height = parseFloat(prompt("Type the rectangle height"));
const rectangule = alert(`How the base measure is ${base}cm and the height measure is ${height}cm, so the Area is ${base*height}`)